# RE-DACT Requirement Progress

## Gradational privacy control

- L1 Direct masking — implemented and selectable
- L2 Sensitive-entity protection — implemented and selectable
- L3 Context generalization — implemented and selectable
- L4 Relationship-safe pseudonymization — implemented and selectable
- L5 Synthetic Twin — implemented for structured CSV / JSON / XLSX and frozen after the 15-gate UI/proof smoke test

## Current format coverage

- PDF / scanned PDF — implemented
- PNG / JPEG — implemented
- TXT / Markdown / canonical safe-RTF — implemented
- CSV / JSON / XLSX — implemented
- DOCX — implemented through Universal Privacy IR with structure-preserving WordprocessingML transformation and embedded-image handling
- Stage-2 MP4 / MOV video — implemented with every-physical-frame change screening, transient-frame promotion into VIDEO_FRAME Privacy IR, temporal region transformation, fail-closed audio stripping and video-specific 13-gate full-timeline verification including an independent all-frame QR payload recovery attack

## Advanced outputs

- Clean protected artifact — implemented
- Cryptographic certificate / complete signed proof package — implemented
- Output-bound annotated evidence export — implemented
- Level 5 Synthetic Twin — implemented for structured datasets

## Remaining completion blocks

Automatic retention expiry is now implemented with user-selectable TTLs, a live lifecycle worker, restart/orphan fail-safe deletion, persistent Ed25519 destruction tombstones and sensitive job-metadata scrubbing.

1. secure deployable web/online profile
2. large-scale multi-format performance/scalability evidence
3. COTS comparison
4. final cybersecurity assault / SBOM / dependency / secrets review
5. final SIH traceability, judge evidence, demo and sanitized submission package

The detector freeze and untouched-holdout rules remain unchanged: Broad PII v3 and the Nemotron holdout evidence must not be tuned retroactively.
